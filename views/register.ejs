<div class="d-flex align-items-center justify-content-center" style="width: 100vw; height: 100vh">
    <div class="container">
        <div class="row">
            <div id="loginBox" class="bg-white border shadow-sm p-4 pt-3 col-md-3 mx-auto">
                <form method="post" action="/register">
                    <h3>Crie sua conta</h3>
                    <% if (typeof(msg) != "undefined") { %>
                        <div class="text-danger"><%= msg %></div>
                    <% } %>
                    <label for="user">Nome de usuário:</label>
                    <input id="user" name="user" type="text" class="form-control"/>
                    <label for="pwd">Senha:</label>
                    <input id="pwd" name="pwd" type="password" class="form-control"/>
                    <label for="pwd">Confirme a senha:</label>
                    <input id="pwd2" name="pwd2" type="password" class="form-control"/>
                    <input type="submit" class="btn btn-primary d-block w-100 fw-bold mb-1" value="Criar conta">
                    <a href="/login" class="btn btn-secondary d-block w-100 fw-bold">Voltar</a>
                </form>
            </div>
        </div>
    </div>
</div>